
import 'package:flutter/material.dart';

class DefaultApp extends StatelessWidget {
  const DefaultApp
({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Sorry, this platform is not yet supported"),
    );
  }
}